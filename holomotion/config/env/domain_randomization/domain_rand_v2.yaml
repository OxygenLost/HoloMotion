# @package _global_

domain_rand:
  # push robots
  push_robots: true
  push_interval_s: [1, 3]
  max_push_vel_xy: 0.5

  # base com
  randomize_base_com: true
  base_com_range:
    x: [-0.05, 0.05]
    y: [-0.05, 0.05]
    z: [-0.05, 0.05]

  # link mass
  randomize_link_mass: true
  link_mass_range: [0.8, 1.2]

  # pd gain
  randomize_pd_gain: true
  kp_range: [0.9, 1.1]
  kd_range: [0.9, 1.1]

  # friction
  randomize_friction: true
  friction_range: [0.3, 1.6]

  # base mass
  randomize_base_mass: true
  added_mass_range: [-1.0, 3.0]

  # rfi
  randomize_torque_rfi: true
  rfi_lim: 0.1
  randomize_rfi_lim: true
  rfi_lim_range: [0.9, 1.1]

  # control delay
  randomize_ctrl_delay: true
  ctrl_delay_step_range: [0, 2] # integer max real delay is 90ms

  # joint default position
  randomize_joint_default_pos: true
  joint_default_pos_range: [-0.01, 0.01]  # radians, bias range for default joint positions
